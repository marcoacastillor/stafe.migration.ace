

CREATE COMPUTE MODULE FSFB_Adapter_DB_REQ_Compute2
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		DECLARE refInp REFERENCE TO InputRoot;
		MOVE refInp LASTCHILD;
		
		PASSTHRU('UPDATE ADMSALUD.AGFA_ADT SET AGFA_ADT.MSH_STATUS = ? WHERE AGFA_ADT.MSH_ID = ?', 'N',refInp.AGFA_ADT.MSH_ID);		
		RETURN FALSE;
	END;
END MODULE;
